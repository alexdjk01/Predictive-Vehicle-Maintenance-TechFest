[project]
name = "pvmt"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["fastapi", "uvicorn[standard]", "pydantic", "numpy", "pandas", "scikit-learn"]

[tool.setuptools]
package-dir = {"" = "src"}   # ← src layout

[tool.setuptools.packages.find]
where = ["src"]
